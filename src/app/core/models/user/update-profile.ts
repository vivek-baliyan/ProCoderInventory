export interface UpdateProfile {
  userId: string;

  firstName: string;
  lastName: string;
  profileImageUrl: string;
  bio: string;
  dateOfBirth: string;
  phoneNumber: string;
  address: string;
  country: string;
  state: string;
  city: string;
  postalCode: string;
}
